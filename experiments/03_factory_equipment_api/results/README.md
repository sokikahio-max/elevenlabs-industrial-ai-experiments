# Results

This phase does not commit machine-specific runtime latency values.

When `api_client_demo.py` is run locally it prints the response and measured
HTTP request latency for each equipment query.

Later experiments can capture repeated measurements in structured CSV files
for statistical analysis.
